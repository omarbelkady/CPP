#ifndef OMAR_H
#define OMAR_H


class Omar
{
    public: 
        Omar();
        //A destructor is a member function that is called automatically when the object
        //is out of scope(meaning we have lost the privellege because of the execution/control flow of the program)
        //A second way to destructor is a call to explicitly delete it. We call a destructor by placing a tilde in front
        //of the class name.
        ~Omar();
    private:
        
    protected:
  
};


#endif
